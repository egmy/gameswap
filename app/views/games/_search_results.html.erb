<div class="list-group" id="myList" role="tablist">
  <% @games.each do |g| %>
    <a class="list-group-item list-group-item-action" id="g<%= g.id.to_s %>-list" data-bs-toggle="list" href="#g<%= g.id.to_s %>" role="tab" aria-controls="g<%= g.id.to_s %>"><%= g.title %></a>
  <% end %>
</div>
